#include <stdlib.h>
#include<iostream>
using namespace std;
 
int main()
{
    int num, c;
   
    for(c = 1; c <= 20; c++)
    {
        num = 1 + rand() % (101);
        cout << num<< " ";
    }
    
    return 0;
}
